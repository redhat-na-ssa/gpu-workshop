# NOTE:  Prior to executing ansible, please copy this file.  Afterwards, edit the copied file

[azure]
<changeme aws_dns_host/ip>  ansible_connection=ssh  ansible_ssh_private_key_file=/path/to/.ssh/<changeme private_key>  ansible_user=azureuser

[aws]

[local]
#192.168.122.<changeme> ansible_connection=ssh  ansible_user=azureuser ansible_sudo_pass=<changeme>
